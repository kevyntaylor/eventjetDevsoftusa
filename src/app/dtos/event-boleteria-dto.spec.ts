import { EventBoleteriaDTO } from './event-boleteria-dto';

describe('EventBoleteriaDTO', () => {
  it('should create an instance', () => {
    expect(new EventBoleteriaDTO()).toBeTruthy();
  });
});
